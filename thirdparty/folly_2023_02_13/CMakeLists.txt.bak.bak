# if cmake type is debug
# if (CMAKE_BUILD_TYPE STREQUAL "Debug")
    # include(${CMAKE_CURRENT_SOURCE_DIR}/bin/debug/lib/cmake/folly/folly-config.cmake)
# elseif(CMAKE_BUILD_TYPE STREQUAL "Release")
    # include(${CMAKE_CURRENT_SOURCE_DIR}/bin/release/lib/cmake/folly/folly-config.cmake)
# else()
    # message(FATAL_ERROR "CMAKE_BUILD_TYPE must be Debug or Release")
# endif()
